1) App shell

App entry + environment config (dev/stage/prod)

Navigation structure (stack/tabs) with deep linking

Global state pattern (lightweight by default)

Feature/module folder structure you can scale

2) Design system + branding

Theme tokens (colors, typography, spacing, radii, shadows)

Component library: Button, Text, Input, Card, Modal, Toast, Loader, Empty states

Dark mode support

Icon system + asset pipeline

3) Auth (optional but pluggable)

Email/password + social login hooks (Google/Apple) if needed

Secure token storage strategy

Session management: refresh, logout, lock screen, re-auth prompts

Auth guard / protected routes

“Forgot password”, email verification templates

4) Networking + data

API client with interceptors (auth headers, retry, refresh)

Typed models + validation

Caching strategy (query cache + offline support optional)

File upload/download helpers

5) Error handling + observability

Global error boundary

Logging levels + redaction of sensitive data

Crash reporting integration point

Analytics events with a consistent naming convention

6) Quality + delivery

Testing setup (unit + UI smoke tests)

Linting/formatting + pre-commit hooks

CI pipeline template (build, test, deploy)

Release flows (versioning, changelog, build numbers)

Feature flags (even a simple wrapper helps a lot)

7) Compliance basics

Privacy-friendly defaults (consent gating for analytics if required)

“Delete account/data” pathway hooks

Accessibility baseline

8) Translation

Translation from english to norwegian. 